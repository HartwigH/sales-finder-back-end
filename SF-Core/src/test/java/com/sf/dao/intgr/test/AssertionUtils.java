package com.sf.dao.intgr.test;

import com.sf.beans.ProductDto;
import com.sf.model.Product;
import org.junit.Assert;

public class AssertionUtils {

    public static void assertDto2ModelMapping(ProductDto ProductDto, Product product) {
//        Assert.assertTrue(product.getProduct_name().equals(ProductDto.getProduct_name()));
//        Assert.assertTrue(product.getProduct_url().equals(ProductDto.getProduct_url()));
//        Assert.assertTrue(product.getProduct_img_url().equals(ProductDto.getProduct_img_url()));
//        Assert.assertTrue(product.getProduct_data_id().equals(ProductDto.getProduct_data_id()));
    }

}
